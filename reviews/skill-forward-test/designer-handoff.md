# Persistent Labs product directory — designer handoff

Use exactly five product entries: FireFlow, Unfazed.dev, Lanni, PrivateInference and Galactica. Frame the directory with Persistent Labs graphite `#1C1E21`, warm gold `#FBD78E`, white and paper `#F6F6F6`; let each product retain its own signature. Use “by Persistent Labs” quietly beneath each product lockup or in its footer, separated by at least one line of clear space.

The parent lockup is the supplied infinity ribbon plus **Persistent Labs**, two words, set in Red Hat Display 500. The supplied SVG is the ribbon alone: construct the text portion using the specified font. Keep one ribbon-height clear space, a half-ribbon-height gap to the name, a ribbon at least 40px wide and a complete lockup at least 180px wide. Identification is solid white on graphite or solid graphite on white.

| Directory entry | Identity and wordmark | Primary colors | Practical exceptions |
| --- | --- | --- | --- |
| **FireFlow** — Orchestration, persistent memory & chat UX | Set **FireFlow** in Red Hat Display 700. `fireflow.svg` is a proposed schematic F, not an extracted official logotype; minimum symbol 24px, clear space one node width. Lowercase italic “fireflow” belongs only to an approved original logo. | Orange `#F7931A` on dark; deep orange `#8C4900` for text on white; tint `#FFF0DE`. Orange filled actions use graphite labels. | One directory entry contains the FireFlow orchestration engine, MemoryTree and FlameChorus. MemoryTree sky `#8ECFFB` / deep blue `#215F8B` and FlameChorus gold `#FBD78E` / gold ink `#76580D` only identify components. Actions remain orange. Show execution, provenance and conversation state; do not create separate product cards for components. |
| **Unfazed.dev** — Deployment for agents | Portfolio name is **Unfazed.dev**; preserve the lowercase **unfazed** wordmark and supplied unimpressed face. `unfazed.svg` is the face. Minimum face 24px; clear space half its height. | White `#FFFFFF`, product ink `#141414`, source blue `oklch(0.55 0.16 256)` (sRGB approximation `#2570CC`), dark-surface companion `#8AB8F6`, tint `#EBF2FC`. | Use system sans and system mono, not Red Hat. Preserve 680px reading width, flat white composition and 6px corners. On light surfaces the blue action uses **white** labels; hover `#1D5BA7`, pressed `#174A89`. Keep commands legible and locally scrollable. Avoid gold pills, glossy cards and animated faces. |
| **Lanni** — An agent for getting things done | **Lanni**, title case, Red Hat Display 600. `lanni.svg` is a proposed action-L mark: minimum 24px, clear space one dot diameter. | Coral `#FF6741`, deep coral `#A6381C` for text on white, tint `#FFF0EB`; graphite labels on filled coral actions. | Built on FireFlow, with its own identity. Use 24px conversation panels, 16px message bubbles and pill actions. Distinguish draft, awaiting review, running, complete and failed. Availability is not verified; do not label it “live” or “available now.” |
| **PrivateInference (PI)** — Upcoming brand | Full lockup: supplied bold vector **π** plus **PrivateInference** in Red Hat Display 700. **PI** is a verbal abbreviation. Minimum symbol 24px; clear space one stem width (16/96 of its viewBox). | Proposed lavender `#C4B5FD`, violet ink `#6544A2` on white, tint `#F2EDFF`; graphite and white grounds. | Label it **upcoming**; palette, positioning and interface treatment are proposals. Use vector π, never a font glyph or “PI” drawn to imitate it. Keep it monochrome; avoid shield, padlock or security-seal treatments. No unverified privacy, deployment or availability claims. |
| **Galactica** — Privacy technology across AI & blockchain | Use complete supplied **Galactica.com** outlined lockup, including `.com` and bold orange dot `#F7931A`. Minimum width 160px, clear space one capital-height. The dot is not a standalone logo. | Proposed copper UI accent `#F4A77A` on graphite, copper ink `#97502A` on white, tint `#FFF1E8`. Logo dot remains orange, distinct from copper. | Label Galactica **live**. Supplied lockup follows owner direction; copper, Red Hat interface type and layouts are family adaptations, not measured live-site rules. Keep AI and blockchain editorial lanes distinct. Do not conflate Galactica with PI or infer shared technical architecture. |

Use a consistent card structure: product mark/name, descriptor, status where established and a clear destination. Use neutral grounds and product accents for identity. Product-specific previews may retain their geometry; Unfazed’s flat treatment is especially important. Concepts should remain labelled in the design file. Do not invent destination links, launch readiness, feature counts or privacy guarantees.

Shared text uses Red Hat Display for headings, Red Hat Text for reading and controls, and Red Hat Mono for technical identifiers. Use 16px body, 14px supporting copy, 1.6 body leading, left alignment and bounded reading widths. Start with 24px card padding, 16–32px gaps, 20px mobile gutters and one column when content no longer fits. Controls need a 44px hit area, visible focus, named states and reduced-motion support.

Assets are under the parent skill’s `assets/`; load `assets/fonts.css` alongside `assets/tokens.css`, preserving relative paths. Use `data-product` for identity and `data-surface` for foregrounds. Inline `currentColor` SVGs with an approved foreground; external SVG images do not inherit surrounding text color. Galactica.com is the only complete outlined wordmark supplied; construct the other text lockups from documented typography without redrawing symbols.

This is a written handoff, not a rendered screen. Actual layout, narrow-screen readability, contrast, keyboard behavior and interaction states still need verification on the built directory.
